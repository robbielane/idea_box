<h3><%= @image.name %></h3>
<p> <img class='idea_image' src="<%= @image.link %>"></p>

<%= link_to 'Edit', edit_admin_image_path(@image), class: 'btn orange' %>
<%= link_to 'Delete', admin_image_path(@image), method: :delete, class: 'btn red' %>
<br><br>
